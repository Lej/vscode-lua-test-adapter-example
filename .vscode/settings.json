{
  // By default Lua is called using `lua` when tests are executed.
  // Use "luaTestAdapter.luaExe" to override this behavior.

  // To specify an absolute path use something like this:
  //"luaTestAdapter.luaExe": "C:/Program Files/Lua/lua-5.1.5_Win64_bin/lua5.1.exe",

  // To specify a relative path use something like this:
  //"luaTestAdapter.luaExe": "${workspaceFolder}/Lua/lua-5.1.5_Win64_bin/lua5.1.exe",
}